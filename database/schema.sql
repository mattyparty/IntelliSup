DROP DATABASE IF EXISTS passport_demo;
CREATE DATABASE passport_demo;
-- commented these out, waiting to see how is works with the config.json file, Might need to only use one database
-- drop database if exists intellissup
-- CREATE DATABASE passport_demo;
